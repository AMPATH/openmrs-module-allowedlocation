<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.openmrs.module</groupId>
    <artifactId>allowedlocation</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Allowed Location Module</name>
    <description>Extends the Data Filter module to scope org.openmrs.Location, including the login
        location picker, to the locations listed in a user's allowed_location user property, as an
        alternative to per-user datafilter_entity_basis_map grants.</description>

    <organization>
        <name>OpenMRS</name>
        <url>http://openmrs.org</url>
    </organization>

    <scm>
        <connection>scm:git:git@github.com:openmrs/openmrs-module-allowedlocation.git</connection>
        <developerConnection>scm:git:git@github.com:openmrs/openmrs-module-allowedlocation.git</developerConnection>
        <url>https://github.com/openmrs/openmrs-module-allowedlocation.git</url>
        <tag>HEAD</tag>
    </scm>

    <modules>
        <module>api</module>
        <module>omod</module>
    </modules>

    <properties>
        <javaCompilerVersion>8</javaCompilerVersion>
        <openmrsPlatformVersion>2.8.9</openmrsPlatformVersion>
        <datafilterVersion>2.2.0</datafilterVersion>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.openmrs.api</groupId>
                <artifactId>openmrs-api</artifactId>
                <version>${openmrsPlatformVersion}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.openmrs.module</groupId>
                <artifactId>datafilter-api</artifactId>
                <version>${datafilterVersion}</version>
                <scope>provided</scope>
            </dependency>
            <!-- Test dependencies -->
            <dependency>
                <groupId>org.openmrs.api</groupId>
                <artifactId>openmrs-api</artifactId>
                <version>${openmrsPlatformVersion}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.openmrs.test</groupId>
                <artifactId>openmrs-test</artifactId>
                <version>${openmrsPlatformVersion}</version>
                <type>pom</type>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.openmrs.module</groupId>
                <artifactId>datafilter-api</artifactId>
                <version>${datafilterVersion}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.8.1</version>
                    <configuration>
                        <target>${javaCompilerVersion}</target>
                        <source>${javaCompilerVersion}</source>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>2.6</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>test-jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.openmrs.maven.plugins</groupId>
                    <artifactId>maven-openmrs-plugin</artifactId>
                    <version>1.0.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <!-- datafilter adds hibernate's filter annotations to entity classes at
                             startup using reflection on java.lang.Class and java.lang.reflect.Field,
                             which the module system denies from jdk 9 onwards unless those packages
                             are opened; without these flags it fails with an
                             InaccessibleObjectException out of Util.addAnnotationToClass. A server
                             running datafilter needs them too, see the Requirements section of the
                             README.

                             They are necessary but not sufficient for AllowedLocationFilterTest,
                             which is context sensitive: on this platform version it also needs a
                             datafilter newer than 2.2.0, whose FullTextQueryCreatedEventListener
                             references classes core 2.8 removed, and even then datafilter's own
                             BaseFilterTest does not get through setup. The rules that test covers
                             are also covered, without a database, by
                             AllowedLocationAccessUtilTest. -->
                        <argLine>--add-opens java.base/java.lang=ALL-UNNAMED
                            --add-opens java.base/java.lang.reflect=ALL-UNNAMED</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>2.4</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <repositories>
        <repository>
            <id>openmrs-repo</id>
            <name>OpenMRS Nexus Repository</name>
            <url>https://mavenrepo.openmrs.org/public</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>openmrs-repo</id>
            <name>OpenMRS Nexus Repository</name>
            <url>https://mavenrepo.openmrs.org/public</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <profiles>
        <!-- Deploy target for the publish-github-packages workflow. AMRS consumes every
             AMPATH built module from the one openmrs-content-amrs package feed, so this
             module deploys there rather than to its own repository's feed. -->
        <profile>
            <id>github-packages</id>
            <distributionManagement>
                <repository>
                    <id>O3-amrs</id>
                    <name>AMRS GitHub</name>
                    <url>https://maven.pkg.github.com/AMPATH/openmrs-content-amrs</url>
                </repository>
                <snapshotRepository>
                    <id>O3-amrs</id>
                    <name>AMRS GitHub</name>
                    <url>https://maven.pkg.github.com/AMPATH/openmrs-content-amrs</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>
    </profiles>

</project>
